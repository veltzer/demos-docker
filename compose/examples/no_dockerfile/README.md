# No Dockerfile specification

This example shows that
    `$ docker-compose build`
will look for a file called `Dockerfile` in the current folder if one is not stated
explicitly in the `docker-compose.yml` file.
