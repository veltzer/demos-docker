# Security Running as User with Write Capability

Demonstrate how to run inside docker as a non root user.

Hints:
* You can use the following commands in your Dockerfile:
    `RUN chown`
    `WORKDIR`
    `USER`
